<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="3dip" />
            <gradient
                android:startColor="@color/heading_bg"
                android:centerColor="@color/heading_bg"
                android:centerY="0.75"
                android:endColor="@color/heading_bg"
                android:angle="270"
                />
        </shape>
    </item>

    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <corners android:radius="3dip" />
                <gradient
                    android:startColor="@color/skip_color"
                    android:centerColor="@color/skip_color"
                    android:centerY="0.75"
                    android:endColor="@color/skip_color"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="3dip" />
                <gradient
                    android:startColor="@color/skip_color"
                    android:centerColor="@color/skip_color"
                    android:centerY="0.75"
                    android:endColor="@color/skip_color"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
    </layer-list>